apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd-image-updater-system

resources:
  - ../../base

# Dev-specific patches can go here if needed
# For example, different AWS account IDs, regions, or intervals
