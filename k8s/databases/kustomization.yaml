apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gamemetrics

resources:
  - timescaledb-deployment.yaml
  - timescaledb-public-lb.yaml
  - events-schema-job.yaml

  # Removed secretGenerator as secrets already exist
# Note: commonLabels removed due to immutable selector conflicts in Deployments
